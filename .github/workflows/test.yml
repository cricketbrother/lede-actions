name: test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Before
        run: df -h

      - name: Free Disk Space
        uses: endersonmenezes/free-disk-space@main

      - name: After
        run: df -h
