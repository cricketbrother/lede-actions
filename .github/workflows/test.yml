name: test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: OS Version
        run: cat /etc/os-release
        
      - name: Disk Usage
        run: df -h

      - name: Disk List
        run: sudo fdisk -l
