name: test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: OS Version
        run: cat /etc/os-release
        
      - name: Disk Usage
        run: df -h
