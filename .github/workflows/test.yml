name: test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: OS Version
        run: cat /etc/os-release
        
      - name: Disk Usage
        run: df -h

      - name: Disk List
        run: sudo fdisk -l

      - name: Switch
        run: cd /mnt

      - name: Checkout
        uses: actions/checkout@main

      - name: Info
        run: |
          pwd
          ls -l

      - name: Move
        run: cp -R ./ /mnt

      - name: Info1
        run: |
          cd /mnt
          pwd
          ls -l
          
